"""
App data package — Python character map modules only.

NOTE: JSON lookup tables and seed files are NOT here.
  JSON lookup tables  →  data/lookup_tables/   (project root, plain directory)
  Seed JSON           →  data/seed/             (project root, plain directory)
  GeoNames dataset    →  data/geonames/         (project root, plain directory)
The root data/ directory has no __init__.py and is NOT a Python package.
"""
